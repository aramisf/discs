PLAYFAIR:

- Quebrar cifra playfair com chaves de ateh 6 caracteres por forca bruta.



Premissas:

  - Todos os caracteres da entrada nao sao acentuados;
  - Os unicos caracteres validos para a entrada sao [A-Za-z]
  - i's e j's serao todos substituidos por i;


Instrucoes de uso:

  - Para executar os testes:
    `make teste';

  - Para criar o arquivo executavel:
    `make';

  - Para remover os arquivos criados:
    `make clean';

  Depois de criado o executavel, voce pode executa-lo assim:
    `./playfair <ARQ1> <ARQ2>'

    Onde:
      - <ARQ1> eh o arquivo contendo o texto a ser decifrado;
      - <ARQ2> eh o arquivo contendo a lista de possiveis chaves para decifrar
        o texto contido em <ARQ1>;


O programa gera automaticamente uma lista sugerida contendo palavras da lingua
portuguesa. Mudei a opcao do comando `make lista', e agora basta rodar `make'.
